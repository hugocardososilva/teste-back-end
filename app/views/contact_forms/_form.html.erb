<%= form_with(model: contact_form, local: true, id: "contact_form") do |form| %>
  <% if contact_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_form.errors.count, "error") %> prohibited this contact_form from being saved:</h2>

      <ul>
        <% contact_form.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, for: "name"  %>
    <%= form.text_field :name, id: "name" %>
  </div>

  <div class="field">
    <%= form.label :email, for: "email"  %>
    <%= form.text_field :email, id: "email" %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message %>
  </div>

  <div class="actions">
    <%= form.submit  %>
  </div>
<% end %>
